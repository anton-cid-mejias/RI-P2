package es.udc.fic.mri_searcher;

import org.apache.lucene.index.IndexWriterConfig.OpenMode;

public class IndexerUI {
    
    public static void main(String[] args) {
	/*
	 * 	-openmode openmode (el open mode será append, create, o create_or_append)
		-index pathname (ruta de la carpeta que contiene o contendrá el índice)
		-indexingmodel default | jm lambda | dir mu (indica el modelo de RI en la creación del índice)
		-coll pathname (ruta de la carpeta que contiene los archivos  de la colección)
	 */
	
	
	
	String openmodeString = null;
	OpenMode openmode = OpenMode.CREATE_OR_APPEND;
	
	
    }

}
